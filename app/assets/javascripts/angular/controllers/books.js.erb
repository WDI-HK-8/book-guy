app.controller('BooksCtrl', ['$scope', '$http', '$location', function($scope, $http, $location){

  var url = "<%= ENV['URL'] %>" || "https://localhost:3000";

  $http.get(url + '/books').success(function(response){
    console.log(response);
    $scope.books = response;
  });
}]);

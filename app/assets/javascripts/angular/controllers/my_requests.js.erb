app.controller('MyRequestsCtrl', ['$scope', '$http', '$location', '$auth', function($scope, $http, $location, $auth){

  var url = "<%= ENV['URL'] %>" || "http://localhost:3000";

  $http.get(url + '/requests').success(function(response){
  });

}]);

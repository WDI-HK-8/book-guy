var app = angular.module('book-guy', ['ngRoute', 'ng-token-auth', 'ui.bootstrap', 'smart-table']);

app.config(['$routeProvider', '$authProvider', function($routeProvider, $authProvider) {
  // Configure Angular Routes
  $routeProvider.
    when('/', {
      templateUrl: "<%= asset_path('static_pages/index.html') %>",
      controller: 'StaticPagesCtrl'
    }).
    when('/mybooks', {
      templateUrl: "<%= asset_path('static_pages/mybooks.html') %>",
      controller: 'MyBooksCtrl'
    }).
    when('/myrequests', {
      templateUrl: "<%= asset_path('static_pages/myrequests.html') %>",
      controller: 'MyRequestsCtrl'
    })

  $authProvider.configure({
    // By default, you only need to configure apiUrl
    // Note that if you put a '/' at the end of the link, there will be errors when calling the api
    apiUrl: "<%= ENV['URL'] || 'http://localhost:3000' %>"
  })
}]);
